import GameClient from "../_components/game-client";

export default function GamePage() {
  return (
    <main className="w-full h-screen bg-gradient-to-b from-blue-50 to-blue-200 overflow-hidden">
      <GameClient />
    </main>
  );
}
